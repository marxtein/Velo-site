# VeloAlpha 网站修改报告

**日期**：2026-06-05
**项目**：`E:\work\tecent\Velo_site_0.1`（GitHub Pages，veloalpha.cn）

---

## 1. 文案修改

| 位置 | 改前 | 改后 |
|---|---|---|
| 首页 hero 角标 | `VELOALPHA · MMXXVI · OPEN SOURCE` | `VELOALPHA · EST. 2026 · BEIJING` |
| 使命页 / 首页 meta | 算出人类能源**的**最优物理拓扑 | 算出人类能源最优物理拓扑（去「的」） |
| 使命页模块层 | `ptEFIT` → `GPU`；`CCS-GMEC` → `C++` | |
| 使命页星图 | `放电`→中心改`聚变堆放电`；`聚变反应`移到外围；`湍流输运`→`输运` | |

## 2. 导航

- **去掉**「关于我们」下拉里的`团队介绍 · Team`（全站）。
- `联系我们 · Contact` → `坐标 · Location`（全站）。
- **修复下拉菜单秒关**：标题与菜单间有 0.6rem 死区,鼠标移过去就丢 `:hover`。加隐形 hover 桥跨过缝隙 + 0.12s 关闭宽限。

## 3. 首页排版（关于我们 / 核心产品）

- 「关于我们」改成**编辑式分栏**：左=标题+融资徽章,右=中文正文(teal 竖线+橙标记),去掉空荡的装饰光环。
- 右栏加宽（`0.66/1.34`),整段高度降低。
- 加**双语**：每段中文配英文译文(中亮英暗层级),产品卡 + 场景 + footer 均双语。
- 清理失效 CSS（`intro-visual/ring/core` 等）。

## 4. 星图动效（使命页）

- 中心`聚变堆放电`改为**静态发光**(去掉脉冲/缩放)。外围节点静态辉光。
- （中途试过"能量汇聚→放电"动效,按反馈撤回,只留发光。）

## 5. 性能优化（不影响视觉）

| 项 | 改前 | 改后 |
|---|---|---|
| 图片 | 13 MB PNG | ~520 KB WebP（按显示尺寸缩放，−96%） |
| 品牌 logo（每页） | 69 KB | 14 KB |
| CSS | 5 文件 203 KB | 合并压缩为 1 个 `app.min.css`（150KB，gzip ~30KB） |
| 字体 | Google Fonts（国内被墙、阻塞首屏） | 拉丁字自托管 woff2；中文走系统字体（PingFang/微软雅黑，0 下载） |
| 图片属性 | 无 | 加 `width/height`（防抖动）+ `loading="lazy"` |
| 单页总重 | ~10 MB | ~0.8 MB（−92%） |

**效果**：国内首屏从"几秒空白"降到秒内;纯静态站抗并发,离 100GB/月带宽上限余量更大。

## 6. 底栏统一

- 全 8 页底栏对齐为同一双语版本：`坐标·Location` / `联系方式·Contact` / `社交媒体·Social` + 英文小字。

---

## 维护须知

- **CSS 源文件仍在**（`styles/enhance/creative/pages/refine.css` = 可编辑源，`app.min.css` = 构建产物）。改样式后需重建：
  ```bash
  cd /e/work/tecent/Velo_site_0.1 && python -c "import rcssmin;open('app.min.css','w',encoding='utf-8',newline='').write(rcssmin.cssmin(''.join(open(f,encoding='utf-8').read()+chr(10) for f in ['styles.css','enhance.css','creative.css','pages.css','refine.css'])))"
  ```
- 原 PNG 留作源（WebP 是导出）。无引用的 `assets/veloalpha-mark.svg`（353KB）可删。
- 自托管字体在 `assets/fonts/`。

## 待办（未做）

- **P1 国内 CDN**：已有 ICP 备案,可挂腾讯云/阿里云 CDN 或 COS/OSS 部署 —— 国内速度的根本解,你说"以后再说"。
- 改动均在本地,**尚未 git 提交 / 部署上线**。
